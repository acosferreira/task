<h1>Listing recurrencies</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @recurrencies.each do |recurrency| %>
  <tr>
    <td><%= recurrency.name %></td>
    <td><%= link_to 'Show', recurrency %></td>
    <td><%= link_to 'Edit', edit_recurrency_path(recurrency) %></td>
    <td><%= link_to 'Destroy', recurrency, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Recurrency', new_recurrency_path %>
